# Verification — C-URT-002

- [ ] KBD runtime tests cover annotated success/failure statuses and nested duplicate-slug-safe hierarchy.
- [ ] Installer regression verifies validated focus rendering for launchd and systemd.
- [ ] KBD runtime fmt, clippy, and test gates pass.
- [ ] Prometheus source repair is committed without the dirty main worktree's memory files.
- [ ] BossFang manifest is tracked and matches the active canonical project ID.
- [ ] Previous binaries and service definition are backed up recoverably.
- [ ] Installed binaries report the repaired build and daemon health passes.
- [ ] Migration dry-run matches expected ledger inventory.
- [ ] Apply produces a backup manifest and preserves parent 5/8 plus nested children.
- [ ] Typed mutation advances revision exactly once; duplicate command is idempotent.
- [ ] Full service restart preserves project state and command audit.
- [ ] Compatibility-projection mismatch check returns none.
